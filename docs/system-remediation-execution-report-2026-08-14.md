# A股监控程序系统整改执行报告

> 执行日期：2026-08-14  
> 部署环境：Windows + 东方掘金终端；MySQL、Redis、Grafana、Prometheus、Loki、Tempo、OpenTelemetry 使用 Docker  
> 最终服务版本目录：`deploy/services-v5`

## 1. 执行结论

整改方案的代码、数据修复、服务部署、历史回放与主要验收项已经执行。实时采集、API、Worker、StrategyScanner、Recovery 计划任务和可观测组件均已恢复运行；数据库中没有遗留的 running 历史回填、行情恢复、对子回放或策略回放任务。

系统当前可以继续进入盘中实时验收阶段。仍有 45,615 条 `BAR_COUNT_MISMATCH` 质量记录需要作为下一轮数据覆盖治理任务处理；它们不包含重复、OHLC、成交量或交易时段对齐 critical 错误。

## 2. 实时行情采集

- 实测确认东方掘金 SDK 3.0.186 每个进程/会话最多订阅50个实时代码，不是每交易所50个；
- 分片改为单交易所、每进程最多50只；
- 当前实时池为沪市500只、深市500只，共1,000只；
- 自动启动20个 Worker，验收时为20连接、0断开；
- 分配版本：`live-20260814-500-cap50`；
- 采集计划任务 `AStockMonitor-MarketCollector` 为 Running；
- 当前处于非交易时段，Tick 接收为0属于预期，下一交易时段还需验收覆盖率和延迟。

## 3. 行情恢复与历史数据

### 3.1 行情恢复

- 误报恢复运行4已取消，已写K线未删除；
- 日线存在性统一按 `(symbol, trading_date)` 判断；
- Recovery V2 成功写入 Canonical K线后直接完成恢复项，不再等待已废弃的 `quote_bar`；
- 官方返回空数据和精确区间无重叠数据标记为 `verified_no_bar`；
- 已完成、`verified_no_bar`、`source_expired` 的终态缺口不会被后续检测重复创建；
- 同一 2026-08-04～2026-08-14 范围重新 Dry Run，重复缺口由297降为0；
- 恢复运行6最终完成：5,560项完成、297项 `verified_no_bar`、失败0、下载28,720根、写入14,928根。

### 3.2 历史批次22

- 增加分区 fencing token、独立心跳、递归终止失联子进程、自动重试和来源异常隔离；
- 两个失败分区均已续跑完成，最终5,015/5,015股票完成；
- 本次续跑新增13,061根官方K线；
- 4个东方掘金确认不可用的股票/周期/日期区间已写入 `history_source_exception`；
- 因存在4个开放来源例外，批次22有意保持 `partial`，而不是伪装为完全成功；
- 当前无 running 历史批次和分区。

### 3.3 质量检查

- 检查范围：2026-02-15～2026-08-13；
- 检查K线：36,106,128根；
- 重复、OHLC、成交量、组成数量和交易时段对齐错误：0；
- 开放问题：45,615条，全部为 `BAR_COUNT_MISMATCH`；
- 按周期：60m 15,731、1d 15,730、30m 10,952、5m 3,202；
- 该项作为后续数据覆盖治理工作保留，不应直接批量删除或忽略。

## 4. 证券状态与策略运行

- 东方掘金 `2038-01-01` 未退市哨兵已归一化；
- 当前 `delist_date >= 2038-01-01` 的证券为0；
- 策略空分钟数据改为 Fail Closed，不再对空集合执行 `Min()`；
- Fast/Observe 使用中国交易日和交易时段门禁；
- 非交易时段只执行生命周期维护，不启动全市场 Fast/Observe；
- StrategyScanner 已恢复为 Windows 自动服务，当前生命周期运行正常。

## 5. 对子顶底真实历史回放

回放范围为东方掘金分钟历史权限覆盖内的 2026-02-24～2026-08-13，周期为5m、30m、60m、1d。

| 指标 | 结果 |
|---|---:|
| RunId | 10 |
| 请求/完成股票 | 5,015 / 5,015 |
| 失败股票 | 0 |
| 处理K线 | 35,714,697 |
| 对子命中 | 2,535,152 |
| 归并事件 | 23,931 |

股票池查询已改为直接读取点时 `instrument_daily_status`，不再 UNION 扫描数千万根K线。新增迁移021覆盖索引。历史事件分页接口、事件详情和K线命中详情均已验收。

## 6. 八策略逐时点历史回放与校准

| 指标 | 结果 |
|---|---:|
| RunId | 6 |
| 请求/完成股票 | 5,012 / 5,012 |
| 失败股票 | 0 |
| 规则评估次数 | 84,615,322 |
| 合格观察 | 75,619 |
| 阈值首穿信号 | 59,526 |
| 校准记录 | 120 |

- 8/8策略全部进入校准矩阵；
- 每策略5个阈值、train/validation/all三个分段，共 `8×5×3=120` 条；
- 80条零样本结果被明确记录，不再静默省略；
- 实际产生信号的策略为3个，其余5个在当前条件和区间内为零样本；
- 没有阈值同时通过训练集与验证集的正收益、胜率和最小样本门槛，因此不自动修改线上阈值；
- 报告：`.artifacts/strategy-replay-6.md`；
- 增加 `--recalibrate-run`，可只重算校准和报告，无需重复执行逐时点评估；
- 首次写入不再执行无意义的范围删除，减少并发 gap lock；可恢复锁冲突日志改为摘要输出。

## 7. API、前端与可观测性验收

- `/api/status`：200；
- Swagger UI：200；
- `/api/history/status`：约117～128ms；
- `/stocks/SHSE.600000` 直接访问：200；
- 股票搜索 `query=600000`：返回 `SHSE.600000`；
- 对子 Run 10 分页事件总数：23,931，详情接口正常；
- 策略 Run 6：120条校准、8个策略；
- Grafana、Prometheus、Loki：均返回200；
- Prometheus：加载1个告警组、12条告警规则；
- 前端 TypeScript 检查和生产构建通过；大于500KB的前端 chunk 仅为构建警告，后续可做按图表模块拆包。

## 8. 自动测试

- `.NET Release` 全解决方案构建：0警告、0错误；
- 对子算法自检：通过；
- 实时K线引擎自检：通过；
- Bar生命周期事件V2自检：通过；
- 8策略确定性和空分钟自检：通过；
- Python：29项通过；
- Vue前端生产构建：通过。

## 9. 最终运行状态

- Windows服务：API、Worker、StrategyScanner 均为 Running/Automatic；
- 计划任务：MarketCollector Running、MarketRecovery Running、DailyHistory Ready；
- 数据容器：MySQL、Redis healthy；
- 可观测容器：Grafana、Prometheus、Loki、Tempo、OTel Collector及Exporter均运行；
- 数据库迁移：019、020、021已应用；
- 残留 running 数据任务：策略0、对子0、历史0、恢复0。

## 10. 下一步

1. 下一交易日执行盘中验收：20个采集进程心跳、1,000股Tick覆盖率、端到端延迟、Redis lag、Bar事件和策略/对子通知；
2. 将45,615条 `BAR_COUNT_MISMATCH` 按交易日、停牌状态、上市日期和东方掘金可用性分类，再分31日窗口执行受控补数；
3. 对5个零信号策略复核特征口径和阈值可达性，不能仅通过下调阈值强行产生信号；
4. 优化前端图表包拆分，降低首次加载体积。
